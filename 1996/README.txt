jammed a bunch of "A"s into a string, saw how it crashed (either need native or a vm, not docker)

tuned it up to see if it could pack the right number of bytes of address on to a register

it woried, kinda randomly lol
